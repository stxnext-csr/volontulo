# Fill the values

secret_key: # generate the key using unix "head -c 64 /dev/urandom | base64 -w 0"
allowed_host: # Site's domain. Important in production mode

# Database Credentials
# Example
# db_user: awesome_username
# db_pass: my-ultra-strong-password
# etc.

db_host:
db_port:
db_name:
db_user:
db_pass:


